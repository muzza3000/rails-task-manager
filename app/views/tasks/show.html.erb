<%= link_to 'edit task', edit_task_path %>
<h1><%= @task.title %></h1>
<div class="task-details">
  <p><%= @task.details %></p>
</div>
<p><%= @task.completed ? "☑ This task is complete" : "☐ This task is not completed yet" %></p>
<%= link_to 'Back to list', tasks_path %>
